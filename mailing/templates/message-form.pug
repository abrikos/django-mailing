{% extends 'cabinet.pug'%}
{%block cabinet%}
h3  {% if 'update' in request.resolver_match.url_name %} Update {%else%}Create {%endif%} message
form(method="post")
    {% csrf_token %}
    {{ form.as_p }}
    button(type="submit") Send

{%endblock%}